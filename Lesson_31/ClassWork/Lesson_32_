-- -- Середня запраплата по відділам
-- SELECT dept_avg.department_id, dept_avg.avg_salary
-- FROM (
--     SELECT employees.department_id, AVG(salary) as avg_salary
--     FROM employees
--     GROUP BY department_id
--      ) dept_avg
-- WHERE dept_avg.avg_salary > 5000;

--Співробітники з зарплатою вище середнього
-- SELECT e.first_name, e.salary
-- FROM employees e
-- WHERE salary > (
--     SELECT AVG(salary)
--     FROM employees
--     WHERE department_id = e.department_id
--     );

--Вивести імя, прізвищеб назва відділу та місто де знаходиться відділ
-- SELECT
--     e.first_name,
--     e.last_name,
--     d.depart_name,
--     l.city
-- FROM employees e
-- JOIN departments d ON e.department_id = d.department_id
-- JOIN locations l ON d.location_id = l.location_id
-- ORDER BY l.city, d.depart_name;

-- SELECT
--     e.first_name,
--     e.last_name,
--     e.salary,
--     --e.job_id,
--     j.job_title
-- FROM employees e
-- JOIN jobs j ON e.job_id = j.job_id
-- ORDER BY e.salary DESC
-- LIMIT 5;

--Знайти всіх співробітників що працюють в одному відділі, наприклад в ТОРОНТО

-- SELECT
--     e.first_name,
--     e.last_name,
--     d.depart_name,
--     l.city
-- FROM employees e
-- JOIN departments d ON e.department_id = d.department_id
-- JOIN locations l ON l.location_id = d.location_id
-- WHERE l.city = 'Toronto';

-- Підрахувати скільки співробітників працює в кожній країні.

-- SELECT
--     c.country_name,
--     COUNT(e.employee_id) AS employee_count
--
-- FROM employees e
-- JOIN departments d ON e.department_id = d.department_id
-- JOIN locations l ON d.location_id = l.location_id
-- JOIN countries c ON l.country_id = c.country_id
-- GROUP BY c.country_name
-- ORDER BY employee_count DESC;

--Знайти співробітників, які не є менеджерами

-- SELECT
--     e.first_name,
--     e.last_name,
--     j.job_title
-- FROM employees e
-- JOIN jobs j ON e.job_id = j.job_id
-- WHERE e.employee_id NOT IN (
--     SELECT DISTINCT manager_id
--     FROM employees
--     WHERE e.manager_id IS NOT NULL
--     )
-- ORDER BY e.last_name;

